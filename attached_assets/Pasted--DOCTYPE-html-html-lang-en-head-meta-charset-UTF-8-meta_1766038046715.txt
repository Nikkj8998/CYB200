<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Enterprise Software Development - CybaemTech</title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700;900&family=Space+Mono:wght@400;700&family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Outfit', sans-serif;
            background: #000;
            color: #fff;
            overflow-x: hidden;
        }

        /* Header Navigation */
        .header-nav {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            background: rgba(0, 0, 0, 0.8);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
            padding: 1.2rem 3rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            animation: slideDown 0.8s ease-out;
        }

        @keyframes slideDown {
            from {
                transform: translateY(-100%);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        .logo {
            font-size: 1.5rem;
            font-weight: 700;
            color: #00ff9d;
            letter-spacing: -0.5px;
        }

        .nav-links {
            display: flex;
            gap: 3rem;
            list-style: none;
        }

        .nav-links a {
            color: rgba(255, 255, 255, 0.8);
            text-decoration: none;
            font-size: 0.95rem;
            font-weight: 500;
            transition: all 0.3s ease;
            position: relative;
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 0;
            height: 2px;
            background: linear-gradient(90deg, #00ff9d, #00b8ff);
            transition: width 0.3s ease;
        }

        .nav-links a:hover {
            color: #00ff9d;
        }

        .nav-links a:hover::after {
            width: 100%;
        }

        .contact-btn {
            background: linear-gradient(135deg, #00ff9d, #00b8ff);
            color: #000;
            padding: 0.7rem 2rem;
            border-radius: 50px;
            font-weight: 600;
            cursor: pointer;
            border: none;
            font-size: 0.95rem;
            transition: all 0.3s ease;
            box-shadow: 0 0 30px rgba(0, 255, 157, 0.3);
        }

        .contact-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 40px rgba(0, 255, 157, 0.5);
        }

        /* Hero Section */
        .hero-section {
            min-height: 100vh;
            padding: 8rem 3rem 4rem;
            position: relative;
            overflow: hidden;
            display: flex;
            align-items: center;
            background: #000;
        }

        /* Animated Background */
        .bg-animation {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
            z-index: 0;
        }

        .gradient-orb {
            position: absolute;
            border-radius: 50%;
            filter: blur(100px);
            opacity: 0.3;
            animation: float 20s infinite ease-in-out;
        }

        .orb-1 {
            width: 600px;
            height: 600px;
            background: radial-gradient(circle, #00ff9d, transparent);
            top: -200px;
            left: -100px;
            animation-delay: 0s;
        }

        .orb-2 {
            width: 500px;
            height: 500px;
            background: radial-gradient(circle, #00b8ff, transparent);
            top: 20%;
            right: -150px;
            animation-delay: 3s;
        }

        .orb-3 {
            width: 400px;
            height: 400px;
            background: radial-gradient(circle, #ff00ff, transparent);
            bottom: -100px;
            left: 30%;
            animation-delay: 6s;
        }

        @keyframes float {
            0%, 100% {
                transform: translate(0, 0) scale(1);
            }
            33% {
                transform: translate(50px, -50px) scale(1.1);
            }
            66% {
                transform: translate(-30px, 30px) scale(0.9);
            }
        }

        /* Code Pattern Background */
        .grid-pattern {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
        }

        /* Binary Code Rain */
        .binary-code {
            position: absolute;
            top: -100%;
            font-family: 'Space Mono', monospace;
            font-size: 14px;
            color: rgba(0, 255, 157, 0.3);
            white-space: nowrap;
            animation: codeRain linear infinite;
        }

        @keyframes codeRain {
            0% {
                top: -100%;
                opacity: 0;
            }
            10% {
                opacity: 1;
            }
            90% {
                opacity: 1;
            }
            100% {
                top: 100%;
                opacity: 0;
            }
        }

        /* Circuit Lines */
        .circuit-line {
            position: absolute;
            background: linear-gradient(90deg, transparent, rgba(0, 255, 157, 0.3), transparent);
            height: 1px;
            animation: circuitFlow linear infinite;
        }

        .circuit-line.horizontal {
            width: 200px;
            animation: circuitFlowHorizontal linear infinite;
        }

        .circuit-line.vertical {
            width: 1px;
            height: 200px;
            animation: circuitFlowVertical linear infinite;
        }

        @keyframes circuitFlowHorizontal {
            0% {
                left: -200px;
                opacity: 0;
            }
            50% {
                opacity: 1;
            }
            100% {
                left: 100%;
                opacity: 0;
            }
        }

        @keyframes circuitFlowVertical {
            0% {
                top: -200px;
                opacity: 0;
            }
            50% {
                opacity: 1;
            }
            100% {
                top: 100%;
                opacity: 0;
            }
        }

        /* Code Symbols */
        .code-symbol {
            position: absolute;
            font-family: 'Space Mono', monospace;
            font-size: 60px;
            opacity: 0.05;
            color: #00ff9d;
            animation: symbolFloat 20s ease-in-out infinite;
        }

        @keyframes symbolFloat {
            0%, 100% {
                transform: translate(0, 0) rotate(0deg);
            }
            25% {
                transform: translate(50px, -50px) rotate(90deg);
            }
            50% {
                transform: translate(100px, 0) rotate(180deg);
            }
            75% {
                transform: translate(50px, 50px) rotate(270deg);
            }
        }

        /* Terminal Lines */
        .terminal-line {
            position: absolute;
            left: 0;
            width: 100%;
            height: 20px;
            background: linear-gradient(90deg, 
                transparent, 
                rgba(0, 255, 157, 0.1) 30%, 
                rgba(0, 255, 157, 0.1) 70%, 
                transparent);
            animation: terminalScan 8s ease-in-out infinite;
        }

        @keyframes terminalScan {
            0%, 100% {
                top: 0;
                opacity: 0;
            }
            50% {
                top: 100%;
                opacity: 1;
            }
        }

        .content-wrapper {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
            position: relative;
            z-index: 1;
        }

        .left-content {
            animation: fadeInLeft 1s ease-out;
        }

        @keyframes fadeInLeft {
            from {
                opacity: 0;
                transform: translateX(-50px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        .badge {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            background: rgba(0, 255, 157, 0.1);
            border: 1px solid rgba(0, 255, 157, 0.3);
            padding: 0.6rem 1.5rem;
            border-radius: 50px;
            font-size: 0.85rem;
            font-weight: 500;
            color: #00ff9d;
            margin-bottom: 2rem;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0%, 100% {
                box-shadow: 0 0 0 0 rgba(0, 255, 157, 0.4);
            }
            50% {
                box-shadow: 0 0 0 10px rgba(0, 255, 157, 0);
            }
        }

        .sparkle {
            width: 12px;
            height: 12px;
            animation: sparkle 1.5s infinite;
        }

        @keyframes sparkle {
            0%, 100% {
                transform: scale(1) rotate(0deg);
                opacity: 1;
            }
            50% {
                transform: scale(1.3) rotate(180deg);
                opacity: 0.7;
            }
        }

        h1 {
            font-family: 'Playfair Display', serif;
            font-size: 5rem;
            font-weight: 900;
            line-height: 1.1;
            margin-bottom: 1.5rem;
            letter-spacing: -2px;
        }

        h1 .white-text {
            color: #fff;
        }

        h1 .gradient-text {
            background: linear-gradient(135deg, #00ff9d, #00b8ff, #ff00ff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            animation: gradientShift 5s ease infinite;
            background-size: 200% 200%;
        }

        @keyframes gradientShift {
            0%, 100% {
                background-position: 0% 50%;
            }
            50% {
                background-position: 100% 50%;
            }
        }

        .description {
            font-size: 1.2rem;
            line-height: 1.8;
            color: rgba(255, 255, 255, 0.7);
            margin-bottom: 3rem;
            font-weight: 300;
        }

        .cta-buttons {
            display: flex;
            gap: 1.5rem;
            align-items: center;
        }

        .btn-primary {
            background: linear-gradient(135deg, #00ff9d, #00b8ff);
            color: #000;
            padding: 1rem 2.5rem;
            border-radius: 50px;
            font-weight: 700;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.7rem;
            transition: all 0.4s ease;
            border: none;
            font-size: 1rem;
            cursor: pointer;
            box-shadow: 0 5px 30px rgba(0, 255, 157, 0.3);
            position: relative;
            overflow: hidden;
        }

        .btn-primary::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
            transition: left 0.5s ease;
        }

        .btn-primary:hover::before {
            left: 100%;
        }

        .btn-primary:hover {
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 8px 40px rgba(0, 255, 157, 0.5);
        }

        .btn-secondary {
            background: transparent;
            color: #fff;
            padding: 1rem 2.5rem;
            border-radius: 50px;
            font-weight: 600;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.7rem;
            transition: all 0.3s ease;
            border: 2px solid rgba(255, 255, 255, 0.3);
            font-size: 1rem;
            cursor: pointer;
        }

        .btn-secondary:hover {
            background: rgba(255, 255, 255, 0.1);
            border-color: rgba(255, 255, 255, 0.5);
            transform: translateY(-3px);
        }

        .arrow {
            display: inline-block;
            transition: transform 0.3s ease;
        }

        .btn-primary:hover .arrow {
            transform: translateX(5px);
        }

        /* Right Content - Animated Visual */
        .right-content {
            position: relative;
            animation: fadeInRight 1s ease-out;
        }

        @keyframes fadeInRight {
            from {
                opacity: 0;
                transform: translateX(50px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        .visual-container {
            position: relative;
            height: 550px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        /* Desktop Device */
        .device-desktop {
            position: absolute;
            width: 500px;
            z-index: 3;
            animation: deviceFloat 6s ease-in-out infinite;
        }

        @keyframes deviceFloat {
            0%, 100% {
                transform: translateY(0) rotateY(-5deg);
            }
            50% {
                transform: translateY(-15px) rotateY(-5deg);
            }
        }

        .desktop-frame {
            background: linear-gradient(135deg, #1a1a1a, #2d2d2d);
            border-radius: 12px 12px 0 0;
            padding: 8px;
            box-shadow: 0 30px 80px rgba(0, 0, 0, 0.5);
            border: 2px solid rgba(255, 255, 255, 0.1);
        }

        .desktop-screen {
            background: #000;
            border-radius: 6px;
            overflow: hidden;
            position: relative;
            aspect-ratio: 16/10;
        }

        .desktop-screen iframe {
            width: 100%;
            height: 100%;
            border: none;
        }

        .desktop-topbar {
            background: rgba(20, 20, 20, 0.95);
            height: 28px;
            display: flex;
            align-items: center;
            padding: 0 12px;
            gap: 6px;
        }

        .desktop-dot {
            width: 10px;
            height: 10px;
            border-radius: 50%;
            animation: dotPulse 2s infinite;
        }

        .dot-red { background: #ff5f57; }
        .dot-yellow { background: #ffbd2e; }
        .dot-green { background: #28ca42; }

        @keyframes dotPulse {
            0%, 100% {
                transform: scale(1);
                opacity: 1;
            }
            50% {
                transform: scale(1.2);
                opacity: 0.8;
            }
        }

        .desktop-stand {
            width: 180px;
            height: 6px;
            background: linear-gradient(135deg, #2d2d2d, #1a1a1a);
            margin: 0 auto;
            border-radius: 0 0 6px 6px;
        }

        .desktop-base {
            width: 250px;
            height: 8px;
            background: linear-gradient(135deg, #1a1a1a, #000);
            margin: 0 auto;
            border-radius: 12px;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.5);
        }

        /* Tablet Device */
        .device-tablet {
            position: absolute;
            width: 220px;
            bottom: 60px;
            left: -80px;
            z-index: 2;
            animation: tabletFloat 5s ease-in-out infinite;
            animation-delay: 1s;
        }

        @keyframes tabletFloat {
            0%, 100% {
                transform: translateY(0) rotate(-8deg);
            }
            50% {
                transform: translateY(-12px) rotate(-8deg);
            }
        }

        .tablet-frame {
            background: linear-gradient(135deg, #1a1a1a, #2d2d2d);
            border-radius: 20px;
            padding: 12px;
            box-shadow: 0 25px 60px rgba(0, 0, 0, 0.4);
            border: 3px solid rgba(255, 255, 255, 0.1);
        }

        .tablet-screen {
            background: #000;
            border-radius: 12px;
            overflow: hidden;
            aspect-ratio: 3/4;
            position: relative;
        }

        .tablet-screen iframe {
            width: 100%;
            height: 100%;
            border: none;
        }

        /* Mobile Device */
        .device-mobile {
            position: absolute;
            width: 140px;
            bottom: 80px;
            right: -50px;
            z-index: 4;
            animation: mobileFloat 4s ease-in-out infinite;
            animation-delay: 2s;
        }

        @keyframes mobileFloat {
            0%, 100% {
                transform: translateY(0) rotate(12deg);
            }
            50% {
                transform: translateY(-10px) rotate(12deg);
            }
        }

        .mobile-frame {
            background: linear-gradient(135deg, #1a1a1a, #2d2d2d);
            border-radius: 28px;
            padding: 10px;
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.4);
            border: 3px solid rgba(255, 255, 255, 0.15);
            position: relative;
        }

        .mobile-notch {
            position: absolute;
            top: 10px;
            left: 50%;
            transform: translateX(-50%);
            width: 60px;
            height: 18px;
            background: #000;
            border-radius: 0 0 12px 12px;
            z-index: 10;
        }

        .mobile-screen {
            background: #000;
            border-radius: 22px;
            overflow: hidden;
            aspect-ratio: 9/19.5;
            position: relative;
        }

        .mobile-screen iframe {
            width: 100%;
            height: 100%;
            border: none;
        }

        /* Screenshot Placeholder */
        .device-screenshot {
            width: 100%;
            height: 100%;
            background: #000;
            display: flex;
            flex-direction: column;
            position: relative;
            overflow: hidden;
        }

        /* Website Header in Screenshots */
        .site-header {
            background: rgba(255, 255, 255, 0.98);
            padding: 0.8rem 1.5rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid rgba(0, 0, 0, 0.1);
        }

        .site-logo {
            font-size: 0.9rem;
            font-weight: 700;
            color: #0066cc;
        }

        .site-nav {
            display: flex;
            gap: 1rem;
            font-size: 0.5rem;
            color: #333;
        }

        .site-contact-btn {
            background: #0066cc;
            color: white;
            padding: 0.3rem 0.8rem;
            border-radius: 4px;
            font-size: 0.5rem;
            font-weight: 600;
        }

        /* Website Hero Content */
        .site-hero {
            flex: 1;
            background: linear-gradient(135deg, #001a33 0%, #003366 50%, #001a33 100%);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 2rem;
            position: relative;
            overflow: hidden;
        }

        /* Background Icons */
        .bg-icon {
            position: absolute;
            color: rgba(0, 102, 204, 0.3);
            font-size: 3rem;
            animation: iconFloat 15s ease-in-out infinite;
        }

        .icon-1 { top: 10%; left: 10%; animation-delay: 0s; }
        .icon-2 { top: 20%; right: 15%; animation-delay: 2s; }
        .icon-3 { bottom: 20%; left: 15%; animation-delay: 4s; }
        .icon-4 { bottom: 15%; right: 20%; animation-delay: 6s; }
        .icon-5 { top: 50%; left: 5%; animation-delay: 3s; }
        .icon-6 { top: 40%; right: 8%; animation-delay: 5s; }

        @keyframes iconFloat {
            0%, 100% { transform: translateY(0) rotate(0deg); opacity: 0.3; }
            50% { transform: translateY(-20px) rotate(10deg); opacity: 0.5; }
        }

        .site-hero-content {
            position: relative;
            z-index: 1;
            text-align: center;
        }

        .site-hero h1 {
            font-size: 2rem;
            font-weight: 900;
            color: white;
            margin-bottom: 0.5rem;
            line-height: 1.2;
            letter-spacing: 2px;
        }

        .site-hero .blue-text {
            color: #3399ff;
            display: block;
        }

        .site-hero p {
            font-size: 0.65rem;
            color: rgba(255, 255, 255, 0.8);
            margin-bottom: 1rem;
            line-height: 1.5;
            max-width: 400px;
        }

        .site-hero-btn {
            background: #0066cc;
            color: white;
            padding: 0.6rem 1.5rem;
            border-radius: 6px;
            font-size: 0.65rem;
            font-weight: 600;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            box-shadow: 0 4px 15px rgba(0, 102, 204, 0.4);
        }

        /* Partner Logos */
        .partner-section {
            background: #000;
            padding: 0.8rem;
            display: flex;
            gap: 0.8rem;
            justify-content: center;
            align-items: center;
            overflow-x: auto;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }

        .partner-logo {
            background: white;
            padding: 0.4rem 0.8rem;
            border-radius: 6px;
            font-size: 0.5rem;
            font-weight: 700;
            color: #333;
            white-space: nowrap;
            min-width: 60px;
            text-align: center;
        }

        /* Tablet specific styles */
        .tablet-screen .site-header {
            padding: 0.6rem 1rem;
        }

        .tablet-screen .site-logo {
            font-size: 0.75rem;
        }

        .tablet-screen .site-nav {
            font-size: 0.4rem;
            gap: 0.6rem;
        }

        .tablet-screen .site-hero h1 {
            font-size: 1.3rem;
        }

        .tablet-screen .site-hero p {
            font-size: 0.5rem;
            max-width: 250px;
        }

        .tablet-screen .partner-section {
            padding: 0.5rem;
            gap: 0.5rem;
        }

        .tablet-screen .partner-logo {
            font-size: 0.4rem;
            padding: 0.3rem 0.6rem;
            min-width: 45px;
        }

        /* Mobile specific styles */
        .mobile-screen .site-header {
            padding: 0.4rem 0.6rem;
        }

        .mobile-screen .site-logo {
            font-size: 0.5rem;
        }

        .mobile-screen .site-nav {
            display: none;
        }

        .mobile-screen .site-contact-btn {
            padding: 0.2rem 0.5rem;
            font-size: 0.35rem;
        }

        .mobile-screen .site-hero h1 {
            font-size: 0.9rem;
            letter-spacing: 1px;
        }

        .mobile-screen .site-hero p {
            font-size: 0.35rem;
            max-width: 150px;
            margin-bottom: 0.6rem;
        }

        .mobile-screen .site-hero-btn {
            padding: 0.4rem 1rem;
            font-size: 0.45rem;
        }

        .mobile-screen .bg-icon {
            font-size: 1.5rem;
        }

        .mobile-screen .partner-section {
            padding: 0.4rem;
            gap: 0.4rem;
            flex-wrap: wrap;
        }

        .mobile-screen .partner-logo {
            font-size: 0.3rem;
            padding: 0.2rem 0.4rem;
            min-width: 35px;
        }

        /* Floating Elements */
        .floating-element {
            position: absolute;
            animation: floatRandom 15s ease-in-out infinite;
        }

        .float-1 {
            top: 10%;
            right: -50px;
            width: 80px;
            height: 80px;
            background: linear-gradient(135deg, rgba(0, 255, 157, 0.2), transparent);
            border-radius: 50%;
            animation-delay: 0s;
        }

        .float-2 {
            bottom: 15%;
            left: -40px;
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, rgba(0, 184, 255, 0.2), transparent);
            border-radius: 50%;
            animation-delay: 3s;
        }

        @keyframes floatRandom {
            0%, 100% {
                transform: translate(0, 0) rotate(0deg);
            }
            33% {
                transform: translate(30px, -30px) rotate(120deg);
            }
            66% {
                transform: translate(-20px, 20px) rotate(240deg);
            }
        }

        /* Stats Section */
        .stats-bar {
            display: flex;
            gap: 3rem;
            margin-top: 4rem;
            padding: 2rem 0;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }

        .stat-item {
            text-align: center;
        }

        .stat-number {
            font-size: 2.5rem;
            font-weight: 700;
            background: linear-gradient(135deg, #00ff9d, #00b8ff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 0.5rem;
        }

        .stat-label {
            font-size: 0.9rem;
            color: rgba(255, 255, 255, 0.6);
            font-weight: 500;
        }

        /* Responsive Design */
        @media (max-width: 1024px) {
            .content-wrapper {
                grid-template-columns: 1fr;
                gap: 3rem;
            }

            h1 {
                font-size: 4rem;
            }

            .visual-container {
                height: 450px;
            }

            .device-desktop {
                width: 400px;
            }

            .device-tablet {
                width: 180px;
                left: -60px;
            }

            .device-mobile {
                width: 120px;
                right: -40px;
            }
        }

        @media (max-width: 768px) {
            .header-nav {
                padding: 1rem 1.5rem;
            }

            .nav-links {
                display: none;
            }

            .hero-section {
                padding: 6rem 1.5rem 3rem;
            }

            h1 {
                font-size: 3rem;
            }

            .description {
                font-size: 1rem;
            }

            .cta-buttons {
                flex-direction: column;
                width: 100%;
            }

            .btn-primary,
            .btn-secondary {
                width: 100%;
                justify-content: center;
            }

            .stats-bar {
                flex-direction: column;
                gap: 2rem;
            }

            .visual-container {
                height: 350px;
            }

            .device-desktop {
                width: 300px;
            }

            .device-tablet {
                width: 140px;
                left: -40px;
                bottom: 40px;
            }

            .device-mobile {
                width: 90px;
                right: -30px;
                bottom: 60px;
            }
        }
    </style>
</head>
<body>
    <!-- Header Navigation -->
    <header class="header-nav">
        <div class="logo">CybaemTech</div>
        <nav>
            <ul class="nav-links">
                <li><a href="#services">Services</a></li>
                <li><a href="#industries">Industries We Serve</a></li>
                <li><a href="#about">About Us</a></li>
                <li><a href="#resources">Resources</a></li>
            </ul>
        </nav>
        <button class="contact-btn">Contact</button>
    </header>

    <!-- Hero Section -->
    <section class="hero-section">
        <!-- Animated Background -->
        <div class="bg-animation">
            <div class="gradient-orb orb-1"></div>
            <div class="gradient-orb orb-2"></div>
            <div class="gradient-orb orb-3"></div>
            <div class="grid-pattern">
                <!-- Binary Code Rain -->
                <div class="binary-code" style="left: 10%; animation-duration: 15s; animation-delay: 0s;">
                    01001000 01100101 01101100 01101100 01101111
                </div>
                <div class="binary-code" style="left: 25%; animation-duration: 12s; animation-delay: 2s;">
                    01000011 01101111 01100100 01100101
                </div>
                <div class="binary-code" style="left: 45%; animation-duration: 18s; animation-delay: 5s;">
                    01010011 01101111 01100110 01110100 01110111 01100001 01110010 01100101
                </div>
                <div class="binary-code" style="left: 65%; animation-duration: 14s; animation-delay: 3s;">
                    01000100 01100101 01110110
                </div>
                <div class="binary-code" style="left: 80%; animation-duration: 16s; animation-delay: 7s;">
                    01000001 01001001
                </div>
                <div class="binary-code" style="left: 90%; animation-duration: 13s; animation-delay: 4s;">
                    11010101 10101010
                </div>

                <!-- Circuit Lines -->
                <div class="circuit-line horizontal" style="top: 20%; animation-duration: 8s; animation-delay: 0s;"></div>
                <div class="circuit-line horizontal" style="top: 45%; animation-duration: 10s; animation-delay: 2s;"></div>
                <div class="circuit-line horizontal" style="top: 70%; animation-duration: 12s; animation-delay: 5s;"></div>
                <div class="circuit-line vertical" style="left: 30%; animation-duration: 9s; animation-delay: 1s;"></div>
                <div class="circuit-line vertical" style="left: 60%; animation-duration: 11s; animation-delay: 4s;"></div>
                <div class="circuit-line vertical" style="left: 85%; animation-duration: 13s; animation-delay: 6s;"></div>

                <!-- Code Symbols -->
                <div class="code-symbol" style="top: 10%; left: 15%; animation-delay: 0s;">&lt;/&gt;</div>
                <div class="code-symbol" style="top: 25%; right: 20%; animation-delay: 3s;">{ }</div>
                <div class="code-symbol" style="bottom: 30%; left: 25%; animation-delay: 6s;">( )</div>
                <div class="code-symbol" style="bottom: 15%; right: 15%; animation-delay: 9s;">[ ]</div>
                <div class="code-symbol" style="top: 50%; left: 5%; animation-delay: 12s;">=&gt;</div>
                <div class="code-symbol" style="top: 60%; right: 10%; animation-delay: 15s;">&lt;?&gt;</div>

                <!-- Terminal Scan Line -->
                <div class="terminal-line" style="animation-delay: 0s;"></div>
                <div class="terminal-line" style="animation-delay: 4s;"></div>
            </div>
        </div>

        <div class="content-wrapper">
            <!-- Left Content -->
            <div class="left-content">
                <div class="badge">
                    <svg class="sparkle" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <path d="M12 2L15 9L22 12L15 15L12 22L9 15L2 12L9 9L12 2Z" fill="currentColor"/>
                    </svg>
                    Enterprise Software Solutions
                </div>

                <h1>
                    <span class="white-text">Enterprise<br>Software</span><br>
                    <span class="gradient-text">Development</span>
                </h1>

                <p class="description">
                    Custom software solutions designed to transform your business. 
                    From web development to enterprise systems, we deliver end-to-end 
                    solutions with cutting-edge technology.
                </p>

                <div class="cta-buttons">
                    <button class="btn-primary">
                        Get Free Consultation
                        <span class="arrow">→</span>
                    </button>
                    <button class="btn-secondary">
                        Schedule a Demo
                    </button>
                </div>

                <div class="stats-bar">
                    <div class="stat-item">
                        <div class="stat-number">150+</div>
                        <div class="stat-label">Projects Delivered</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-number">98%</div>
                        <div class="stat-label">Client Satisfaction</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-number">50+</div>
                        <div class="stat-label">Global Clients</div>
                    </div>
                </div>
            </div>

            <!-- Right Content - Responsive Device Mockup -->
            <div class="right-content">
                <div class="visual-container">
                    <!-- Desktop Device -->
                    <div class="device-desktop">
                        <div class="desktop-frame">
                            <div class="desktop-topbar">
                                <div class="desktop-dot dot-red"></div>
                                <div class="desktop-dot dot-yellow"></div>
                                <div class="desktop-dot dot-green"></div>
                            </div>
                            <div class="desktop-screen">
                                <iframe src="https://www.cybaemtech.com/" style="width: 100%; height: 100%; border: none; background: #000;"></iframe>
                            </div>
                        </div>
                        <div class="desktop-stand"></div>
                        <div class="desktop-base"></div>
                    </div>

                    <!-- Tablet Device -->
                    <div class="device-tablet">
                        <div class="tablet-frame">
                            <div class="tablet-screen">
                                <iframe src="https://www.cybaemtech.com/" style="width: 100%; height: 100%; border: none; background: #000; transform: scale(0.8); transform-origin: top left; width: 125%; height: 125%;"></iframe>
                            </div>
                        </div>
                    </div>

                    <!-- Mobile Device -->
                    <div class="device-mobile">
                        <div class="mobile-frame">
                            <div class="mobile-notch"></div>
                            <div class="mobile-screen">
                                <iframe src="https://www.cybaemtech.com/" style="width: 100%; height: 100%; border: none; background: #000; transform: scale(0.5); transform-origin: top left; width: 200%; height: 200%;"></iframe>
                            </div>
                        </div>
                    </div>

                    <div class="floating-element float-1"></div>
                    <div class="floating-element float-2"></div>
                </div>
            </div>
        </div>
    </section>

    <script>
        // Add smooth scrolling
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Add parallax effect to background orbs
        document.addEventListener('mousemove', (e) => {
            const orbs = document.querySelectorAll('.gradient-orb');
            const x = e.clientX / window.innerWidth;
            const y = e.clientY / window.innerHeight;

            orbs.forEach((orb, index) => {
                const speed = (index + 1) * 20;
                const xOffset = (x - 0.5) * speed;
                const yOffset = (y - 0.5) * speed;
                orb.style.transform = `translate(${xOffset}px, ${yOffset}px)`;
            });
        });

        // Animate stats on scroll
        const observerOptions = {
            threshold: 0.5,
            rootMargin: '0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    const statNumbers = entry.target.querySelectorAll('.stat-number');
                    statNumbers.forEach(stat => {
                        const target = stat.textContent;
                        const isPercent = target.includes('%');
                        const number = parseInt(target);
                        let current = 0;
                        const increment = number / 50;
                        const timer = setInterval(() => {
                            current += increment;
                            if (current >= number) {
                                stat.textContent = target;
                                clearInterval(timer);
                            } else {
                                stat.textContent = Math.floor(current) + (isPercent ? '%' : '+');
                            }
                        }, 30);
                    });
                    observer.unobserve(entry.target);
                }
            });
        }, observerOptions);

        const statsBar = document.querySelector('.stats-bar');
        if (statsBar) {
            observer.observe(statsBar);
        }
    </script>
</body>
</html>